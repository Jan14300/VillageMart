// Re-export all locale configuration from i18n/config.ts
export { locales, type Locale, localeNames, defaultLocale, localeDirections, getMessages } from '@/i18n/config';

// Additional config options can be added here if needed